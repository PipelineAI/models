- package: e30e79aamnist
  version: xgboostertagger
  author-email:
  tags: [e30e79aamnist, xgboostertagger, xgboost, model]

# -------------------------------------------------------------------
# base (shared config)
# -------------------------------------------------------------------

- config: base
  operations:
    train:
      description: 'Train'
      cmd: 'pipeline_train'
      flags:
        model_dir:
          description: Model dir
          default: .
    evaluate:
      description: 'Evaluate'
      cmd: 'pipeline_train'
      requires:
        - trained-model
  resources:
    trained-model:
      private: yes
      sources:
        - operation: train
          select:
            - model.ckpt.*
            - checkpoint

- model: e30e79aamnist
  extends: base
  params:
    train_cmd: pipeline_train
    model_desc: e30e79aamnist
    model_name: e30e79aamnist
    model_tag: xgboostertagger
    model_type: xgboost
    model_chip: cpu